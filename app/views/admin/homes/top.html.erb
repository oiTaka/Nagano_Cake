
<div class="container my-5">
 <div class="row">
  <div>
   <h3 class="title-text text">注文履歴一覧</h3>
  </div>
 </div>

 <div class="row mt-5">
  <table class="table table-hover">
   <thead>
    <tr class="table-active text">
     <th>購入日時</t0h>
     <th>購入者</th>
     <th>注文個数</th>
     <th></th>
     <th colspan="3"></th>
    </tr>
   </thead>

   <tboy>
   <% @orders.each do |order| %>
    <tr class="light-text">
     <td><%= link_to order.created_at.strftime('%Y/%m/%d %H:%M:%S'), admin_order_path(order.id), class: 'text-dark' %></td>
     <td><%= order.customer.last_name + order.customer.first_name %></td>
     <td><%= order.order_details.sum(:order_amount) %></td>
     <td></td>
    </tr>
   <% end %>
   </tboy>
  </table>
  <div class="mx-auto"><%= paginate @orders %></div>
 </div>
</div>
